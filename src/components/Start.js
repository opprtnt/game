import styled from "styled-components";

const Start = () => {
  return (
    <>
      <StartWrapper>start</StartWrapper>
    </>
  );
};

const StartWrapper = styled.div`
  color: lightpink;
  font-weight: 700;
  padding: 5px;
  font-size: 1.5rem;
  text-transform: uppercase;
`;

export default Start;
